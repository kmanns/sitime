export default async function decorate(block) {
  // The block has 3 rows:
  // Row 1: 5 cells (link columns with h3 title + ul links)
  // Row 2: 2 cells (contact links + social links)
  // Row 3: 2 cells (copyright + legal links)
  // Structure is already suitable for CSS grid/flex layout.
}
